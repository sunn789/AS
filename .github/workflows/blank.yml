name: HelloWord
on: workflow_dispatch
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello step
        run: |
          echo "this is first line"
          echo "this is the second line"
        
        
